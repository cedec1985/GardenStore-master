spring:
  datasource:
    url: jdbc:postgresql://localhost:8000/gardenstore_db
    username: cedric
    password: Artterrevil-85
    driver-classname: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  main:
    allow-circular-references: true

server:
  port: 8080
